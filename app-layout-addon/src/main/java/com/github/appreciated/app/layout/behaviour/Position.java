package com.github.appreciated.app.layout.behaviour;

/**
 * FIXME I moved this enum out side because its used in several classes. So it should not be an inner enum of a specific class.
 * 
 * Defines positions a layout can be positioned.
 *
 */
public enum Position
{
	TOP, DRAWER
}
